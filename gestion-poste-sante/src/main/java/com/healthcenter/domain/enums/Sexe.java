package com.healthcenter.domain.enums;

/**
 * Énumération pour le sexe biologique.
 * ENUM = type sécurisé (pas de String "homme" vs "Homme" vs "HOMME")
 */
public enum Sexe {
    HOMME,
    FEMME,
}
